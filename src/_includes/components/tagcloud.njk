<ul class="tag-cloud">
  {% for tag in tags %}
    {% set tagUrl %}/explore/{{ tag | slug }}/{% endset %}
    <li><a class="link-button" href="{{ tagUrl | url }}" class="tag">{{ tag }}</a></li>
  {% endfor %}
</ul>